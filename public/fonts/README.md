# Custom fonts

Place **SB Aggro OTF Light** here for the design system to use it.

- Expected filename: `SBAggroOTF-Light.otf`
- Usage: add the `font-aggro` class in Tailwind, or use the CSS variable `var(--font-sb-aggro)`.
